spring:
  application:
    name: commander
  datasource:
    url: ${DB_URL}
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate.ddl-auto: update

springdoc:
  api-docs:
    path: /api-docs